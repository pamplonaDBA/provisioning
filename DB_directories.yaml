--- 
 - name: Including package
   include_vars:
    file: db_install_{{ oracle_version }}.yaml

 - name: create  directories
   file:
    path: '{{ root_directory }}'
    state: directory
    owner: "{{ oracle_user }}"
    group: "{{ oracle_group }}"
    mode: '0775'
   ignore_errors: "{{ ignore}}"

#   with_items:
#      - "{{ root_directory }}"
#      - "{{ scripts_directory }}"
#      - "{{ oracle_inventory }}"
#      - "{{ oracle_base }}"
#      - "{{ stage_dir }}"
#      - "{{ oracle_home }}"
#    ignore_errors: "{{ ignore}}"
#    tags:
#      - create_directories
