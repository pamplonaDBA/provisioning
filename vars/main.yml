ansible_user:                   "svcautoope"
root_user:                      "root"
oracle_user:                    "oracle"
oracle_uid:                     "500"
oracle_gid:                     "500"
oracle_group:                   "dba"
oracle_user_home:               "/home/oracle"
media_dir:                      "/mnt/media"
root_directory:                 "/u01/app"
oracle_inventory:               "{{root_directory}}/oraInventory"
oracle_base:                    "{{root_directory}}/oracle"
stage_directory:                "{{oracle_base}}/stage"
oracle_hostname:                "{{ ansible_hostname|upper }}"
oracle_scripts_dir:             "{{oracle_user_home}}/scripts"
oracle_logs_dir:               "{{oracle_user_home}}/logs"
oracle_home_directory:
  rdbms:                        "{{oracle_base}}/product/{{version}}/dbhome2"
  asm:                          "{{oracle_base}}/product/{{version}}/grid"
response_file:                  "{{type}_{{action}}_{{version}}.rsp"
data_location:          
 FS:                            "/oradata/{{SID}}/data"
 ASM:                           "+{{oracle_hostname}}_DATA'}}"
fra_location:
  FS:                           "/oradata/{{SID}}/data"
  ASM:                          "+{{oracle_hostname}}_FRA'}}"
redo1_location:
  FS:                           "/oradata/{{SID}}/redo"
  ASM:                          "+{{oracle_hostname}}_REDO1'}}"
redo2_location:
  FS:                           "/oradata/{{SID}}/redo"
  ASM:                          "+{{oracle_hostname}}_REDO2'}}"
sysasm_passd:                   "{{SET_sysasm_passd|default('changueme2020',true)}}"
asmdbsnmp_passwd:               "{{SET_asmdbsnmp_passwd|default('changueme2020',true)}}"
oracle_oem_home:                 "{{oracle_base}}/product/em13c"
